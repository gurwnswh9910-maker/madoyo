## 전역 작업 원칙

- 터미널 명령은 백그라운드로 숨겨 실행하지 말고, 사용자가 진행 상황을 볼 수 있게 실행한다.
- 실행 계획서와 할 일 목록은 대화창에 장문으로 그대로 뿌리지 말고, 필요하면 별도 `md` 파일로 정리한다.
- 계획서와 할 일 목록은 한국어로 작성한다. 가능하면 사고 과정과 사용자-facing 설명도 한국어를 우선한다.
- 컨텍스트 관리를 위해 참고 중인 할 일 목록이나 아티팩트가 있으면 수시로 다시 확인한다. 불필요한 혼잣말은 줄이고, 대화가 과도하게 길어지지 않게 유지한다.
- 최신 정보가 필요한 사안은 모델의 기본 지식만 믿고 단정하지 말고, 필요 시 현재 기준으로 다시 확인한다.
- 오류 수정 시 fallback 구현이나 단순 재시도를 기본 해법으로 추가하지 않는다. 기능의 의도를 먼저 파악하고, 실제로 동작하지 않는 근본 원인을 찾아 수정한다.

## 현재 운영 코드 경계

- 현재 로컬 자동화/수동화/반자동화의 기준 경로는 `자동화`, `수동화`, `반자동`, `작동중코드`이다.
- `backend` 폴더는 현재 운영 자동화의 기준 폴더가 아니며, 명시적인 요청 없이 이 폴더를 기준으로 분석하거나 수정하지 않는다.
- 자동화 오류, 수동화 파이프라인, 반자동 업로드, 카피 최적화/채점기 작업은 먼저 위 네 경로에서 실제 import 경로와 실행 파일을 확인한다.

## 미디어수동 파이프라인 운영 지침

- `미디어수동`은 사용자가 직접 넣은 미디어 폴더를 카피 생성 엑셀로 바꾸는 검수용 파이프라인이다. `미디어 소싱 에이전트`와 혼동하지 않는다.
- 입력은 `미디어수동/미디어수동참조/<게시물폴더>/001.jpg`처럼 폴더 1개가 게시물 1개인 구조를 기준으로 한다.
- 미디어 순서는 파일명 정렬 기준이므로 사용자가 원하는 순서대로 `001`, `002`, `003` 번호를 붙이게 안내한다.
- 실행 전에는 반드시 형식 검사를 먼저 한다: `python .\미디어수동\media_manual_pipeline.py --check-only`
- 실행 후에는 결과 엑셀이 반자동 입력 형식인지 검사한다: `python .\미디어수동\media_manual_pipeline.py --validate-output-excel <결과엑셀경로>`
- `내쿠팡링크`는 v1에서 임시 비활성이다. 반자동 업로드 검증은 `--allow-missing-deeplink --dry-run`을 먼저 붙여 실행한다.
- 미디어수동의 RAG는 새 미디어의 visual embedding으로 기존 `visual` 벡터만 조회한다. text/multi fallback을 섞지 않는다.
- 미디어수동 결과가 안정적으로 검수되기 전까지 바로 대량 업로드하지 않고, 단건 또는 소량 dry-run을 먼저 확인한다.
