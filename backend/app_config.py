import os
from dotenv import load_dotenv

# .env 파일 로드 (API 키 등 보안 정보)
load_dotenv()

# ════════════════════════════════════════════════════════════════════════════════
# 1. API 및 시스템 설정
# ════════════════════════════════════════════════════════════════════════════════
GEMINI_API_KEY = os.getenv("GEMINI_API_KEY")
MODEL_NAME = "gemini-3-flash-preview"
EMBEDDING_MODEL = "gemini-embedding-001"

# 병렬 처리 스레드 수
MAX_WORKERS = 25

# 데이터 및 임베딩 저장 경로
BASE_PATH = os.path.abspath(os.path.join(os.path.dirname(__file__), '..'))
STORAGE_PATH = os.path.join(BASE_PATH, 'embeddings.pkl')
CACHE_PATH = os.path.join(BASE_PATH, 'data_cache.pkl')

# ════════════════════════════════════════════════════════════════════════════════
# 2. 사용자 입력 (수시로 변경하는 부분)
# ════════════════════════════════════════════════════════════════════════════════

# 최적화할 원본 카피
ORIGINAL_COPY = """셀프 네일 할땐 이만한게 없음☝️
이거하고 한바퀴만 돌려주면
심플하고 고급스럽게 완성됨💕"""

# 홍보할 제품 정보 (브랜드, 핵심 소구점, 타겟 등 상세할수록 좋음)
PRODUCT_FOCUS = "손가락을 넣고 빙빙 돌리면 샵 퀄리티 자석 네일이 완성되는 자석 네일 전용 회전 장치/키트 (네일샵 원장님도 놀란 신박템, 1초 완성 자석 아트, 똥손도 가능한 전문가급 네일, 집에서 하는 자석 네일 대박)"

# ════════════════════════════════════════════════════════════════════════════════
# 3. 고정 성공 공식 (정적 전략)
# ════════════════════════════════════════════════════════════════════════════════
STATIC_STRATEGIES = [
    ('일상 속 비의도적 발견 (진성)', '전문가나 셀럽이 아닌 일상 속 인물(가족, 친구 등)의 비의도적인 감탄을 통해 가치를 증명하고, 이모티콘을 활용해 진정성을 부여하세요.'),
    ('제3자 검증 통한 우월감 (신뢰)', '엄마, 지인 등 제3자의 구체적 반응을 묘사하고, 그 가치가 왜 발생했는지 이유를 설명하여 똑똑한 소비를 했다는 지적 우월감을 자극하세요.'),
    ('트렌드 비교를 통한 사유유도 (참여)', '대중에게 인지도가 높은 핫플이나 브랜드를 대조 비교하여 시선을 끈 뒤, 개방형 질문을 던져 독자가 능동적으로 생각하게 유도하세요.'),
]
